{
  "name": "librea11y",
  "version": "0.1.1",
  "description": "Suite de 15 applications d'accessibilité, gratuites et hors-ligne",
  "private": true,
  "scripts": {
    "cap:add:android": "npx cap add android",
    "cap:copy": "npx cap copy",
    "cap:sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/core": "6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "6.2.0",
    "@capacitor/android": "6.2.0"
  }
}
